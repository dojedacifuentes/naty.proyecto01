// PF1821 · Módulo 2 · Quiz AE3. Generado desde B2-instrumentos.md (instrumento 3).
// Moodle: Banco de preguntas → Importar → formato GIFT.
$CATEGORY: PF1821-M2/AE3

::AE3-P7:: Si $json.email vale " Ana@Correo.TEST ", la expresión \{\{ $json.email.trim().toLowerCase() \}\} devuelve\: {
~" ana@correo.test "
="ana@correo.test"
~"ANA@CORREO.TEST"
~Un error
####trim() quita los espacios de los extremos y toLowerCase() pasa a minúsculas.
}

::AE3-P8:: Tienes 8 pedidos y necesitas el total vendido por comuna. ¿Qué nodo usas? {
~Merge
=Summarize, agrupando por comuna y sumando el total
~Switch
~Split Out
}

::AE3-P9:: Supabase rechaza una fila con el error invalid input syntax for type integer\: "3 unidades". ¿Qué ocurrió y cómo lo corriges en n8n? {
####Respuesta esperada: a la columna entera cantidad llegó un texto que no es un número (1 punto); se corrige antes de guardar, por ejemplo en Edit Fields con tipo Number y \{\{ parseInt($json.cantidad, 10) \}\}, y validando con un If que la cantidad sea mayor que 0 (1 punto).
}
